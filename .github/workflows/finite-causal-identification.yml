name: finite-causal-identification

on:
  pull_request:
    paths:
      - "verification/finite_causal_identification/**"
      - "foundations/BELLMAN_FINITE_CAUSAL_IDENTIFICATION_AND_DECISION_CERTIFICATION.md"
      - "verification/unsafe_set_reachability/**"
      - "foundations/BELLMAN_UNSAFE_SET_REACHABILITY_AND_CONSTRAINED_SELECTION.md"
      - ".github/workflows/finite-causal-identification.yml"
      - ".github/workflows/unsafe-set-reachability.yml"
      - "README.md"
      - "docs/programme/**"
      - "docs/history/records/FINDINGS_LEDGER.md"
      - "docs/history/records/SUBSTRATE_LEDGER.md"
  push:
    branches:
      - codex/finite-causal-identification

permissions:
  contents: read

jobs:
  exact-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run current causal and pinned historical acceptance
        env:
          PYTHONDONTWRITEBYTECODE: "1"
        run: python3 verification/finite_causal_identification/run_checks.py
