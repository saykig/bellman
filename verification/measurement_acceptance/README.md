# Measurement menu and frozen AFY acceptance

```sh
PYTHONDONTWRITEBYTECODE=1 python verification/measurement_acceptance/acceptance.py --source /external/Aoyagi_2024a_data.txt --recompute --verify-retained
```

Uses AFY's existing requirements and explicitly acquired hash-bound author file.
Preserves aa2f9f2 byte-for-byte except two living guidance files and five CI routes.
Replays its full receipt and inherited mathematics at that pinned edition, then
runs the public exact menu audit normally and under `-O`. This is additive; earlier
acceptance files and receipts are unchanged. `--history`, `--strategic`, `--causal`
retain the corresponding AFY routes and add menu arithmetic checks. Scoped routes
do not claim fresh AFY fitting. Full `--record` is creation-only after sources are
committed. The receipt excludes itself from its source manifest.

The public audit checks exact risks, signal-only optimal action sets and menu
ranking. Full donor receiving is a separate local gate because that existing
repository is private: use research/measurement_decision/checks.py with
`--engine-repository` as documented there. Hosted checks do not claim donor code
execution. Frozen local-receiving.json records that additional run with source
bindings. No data acquisition occurs inside deterministic checks.
