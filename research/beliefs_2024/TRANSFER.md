# Portable result and downstream disposition

9 September 2026. Read-only inspection of remote main, with no downstream edits:

- Writ `4e9b7f49e240e4ca5d6a8df0c6b8ecb060d606e2`,
  [decision-cases contract](https://github.com/saykig/Writ/blob/4e9b7f49e240e4ca5d6a8df0c6b8ecb060d606e2/docs/current/decision-cases.md).
  Its original adapter is pinned separately to Decision Lab 7215b53, CPython 3.13 and
  SciPy 1.17.0, enabling only decision and compatibility for finite-linear-uncertainty.v1.
  Changed mathematical queries require new checking; use-context changes require
  applicability reassessment even if the old mathematical statement remains correct.
- Decision Lab `e5f77dfcf929708951f4673b3f394461ef09c752`, BUILD_2_SPEC.md and
  CERTIFICATE_TRANSPORT_SPEC.md. Build 2 represents a finite rational probability
  polytope and linear objectives; its exact endpoint claims require equal primal/dual
  objectives. The separate identity-skeleton transport adapter explicitly excludes
  statistical learning and requires finite observable histories and a supplied policy.

No installed downstream operation receives this study's whole claim. Our comparison
combines fitted mixture/response parameters, post-action reports used only with a lag,
random session sampling assumptions, nonlinear response probabilities and held-out
scores. A session bootstrap interval is not a supplied exact probability-family constraint.
The per-history prior score is a difference of finite maxima; averaged history endpoints
are only outer bounds for a common prior. Relabelling any of these as the existing static
profile would drop essential distinctions. The transport adapter also cannot infer beliefs,
validate training/target exchangeability or transport a statistical coverage claim.

One could feed separately supplied rational linear subproblems into Build 2, but this
would certify only those subproblems. It would not improve the present end-to-end claim
over the independent rational receiving already retained. There is no demonstrated need
for a cross-repository adapter, new schema family, solver framework or language migration.

The portable Bellman case therefore binds the exact source, specification, parameters,
predictions, receiving evidence and intended use. Its only justified use is inspecting
this retrospective target-prediction comparison under the supplied assumptions. It is not
an authorization to choose an institutional mechanism or to infer pre-action human beliefs.
The local transfer check preserves old mathematical evidence while rejecting reuse after
that use-context change. It is a concrete transfer gap, not a claim that Writ or Decision Lab
have integrated statistical inference.

A future handoff would need a bounded statistical-result contract that keeps training,
target, timing, estimator, uncertainty method, dependence unit and empirical transport
assumptions distinct from arithmetic checks. It should be motivated by an actual downstream
review workflow before building an adapter. Do not add it merely to make this case native.
