# Sequential credibility: inherited CI replay correction

8 September 2026. Additive correction after the source-bound strategic result at
`af7a8fc67cb204b67474eb7749c76c414189a5d5`. This does not change that result or the
earlier [research review](SEQUENTIAL_CREDIBILITY_RESEARCH_REVIEW_2026_09_08.md).

The dedicated hosted shared-consistency check passed. Two inherited aggregate jobs
failed before mathematical evaluation:

1. `longitudinal_causal_policy/run_checks.py` rejects any path outside the completed
   PR #20 task's scope and binds its retained sources to that edition's living docs.
   Its original scope cannot include the strategic additions.
2. `history_migration/checks.py` requires the files in the releases directory to be
   exactly the six published-version manifest entries. The two additional unpublished
   research-transition notes violate that older closed-set condition. The first of
   these notes was already in the preliminary PR #22 draft.

These failures do not falsify the strategic mathematics. They also must not be hidden
under a generic all-CI-passing claim. The repair changes only three workflow
entrypoints and adds a source-preserving aggregate receiver, documentation and evidence.
All mathematical implementations, original runners, reviews, notes and retained
results remain byte-identical to the retained strategic head.

The new aggregate first enforces that identity, allowing only the explicitly named
workflow edits and acceptance additions. It then executes the original strategic,
causal and history runners in exact temporary snapshots where their scope and source
identity assumptions hold. No original assertion is disabled or monkey-patched. The
causal job still runs its full historical chain. Unexpected extra release notes and
changed retained evidence are rejected before replay, with decisive negative controls.

This is a narrow correction to closed-task orchestration, not a new causal result,
general task registry, release publication, or permission to mutate frozen research.
The live causal PR #21 branch is neither changed nor used as a dependency.
The earlier statement that broader causal suites were not rerun remains accurate for
the retained mathematical record; this later acceptance record separately reports the
expanded replay. Likewise the earlier 158-file preservation count describes its
original gate, before two inherited workflow entrypoints were deliberately revised.

Use the [current acceptance command](../verification/sequential_consistency/ACCEPTANCE.md)
for current-tree validation. Direct invocation of the older retained orchestration
continues to reject later workflow bytes by design. This distinction keeps old
evidence correct under its original sources while giving the current PR complete
acceptance coverage.
